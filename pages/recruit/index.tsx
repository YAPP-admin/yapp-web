import React, { ReactElement } from 'react';
import styled from 'styled-components';

function RecruitPage(): ReactElement {
  return <Wrapper>RecruitPage</Wrapper>;
}

const Wrapper = styled.div``;

export default RecruitPage;
